#!/bin/bash

DOMAIN=$1

if [ -z "$DOMAIN" ]; then
  echo "Usage: $0 <domain>"
  exit 1
fi

certbot \
  --nginx \
  --non-interactive \
  --agree-tos \
  -m matthew.stephen.james@gmail.com \
  -d $DOMAIN

nginx -s reload
